# Example extra catalog id. Operators point bake -dockerfile at their
# own file the same way. The default catalog id uses the documented
# universal-class source instead of this file.
FROM alpine:3.20
RUN apk add --no-cache coreutils
