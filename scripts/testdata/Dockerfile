# Tiny fixture for CI smoke. Extra catalog ids use the same scripts/bake
# with --dockerfile. The default catalog id uses the universal-class source.
FROM alpine:3.20
RUN apk add --no-cache coreutils
